# Core NVIDIA libraries
# Note: nvidia-ml-py requires NVIDIA drivers to be installed
nvidia-ml-py>=12.0.0

# NIM SDK packages - these may need to be installed from NVIDIA NGC
# If not available, the examples will run in simulation mode
# nim-sdk  # Uncomment when available from NVIDIA NGC
# nimlib   # Uncomment when available from NVIDIA NGC

# CLI and UI
click>=8.0.0
rich>=13.0.0
typer>=0.9.0

# HTTP and data handling
requests>=2.28.0
pydantic>=2.0.0

# For simulation/testing when NVIDIA libraries are unavailable
psutil>=5.9.0

# Optional development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0